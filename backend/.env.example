# ──────────────────────────────────────────────────────────────
# EduVNU Backend — Environment Variables
# Sao chép thành .env và điền giá trị thực
# ──────────────────────────────────────────────────────────────

# Django
SECRET_KEY=django-insecure-change-me-in-production
DEBUG=False

# ── Database — MS SQL Server ───────────────────────────────────
# HOST khi chạy Docker: dùng host.docker.internal (trỏ về máy Windows)
# HOST khi chạy local:  tên máy (VD: DESKTOP-F59NR4S) hoặc localhost
DB_HOST=host.docker.internal
DB_NAME=Courses
DB_PORT=1433

# SQL Server Login (cần tạo login trên SQL Server — xem README)
# Để trống = dùng Windows Auth (chỉ hoạt động khi chạy trực tiếp trên máy)
DB_USER=sa
DB_PASSWORD=YourStrong!Password

# ── Payment ────────────────────────────────────────────────────
VNPAY_TMN_CODE=GR1Z38XD
VNPAY_HASH_SECRET=T1SXW4II9HOG8ZKK8DF2UJF1I59YWB29
